# 샘플

이 폴더는 템플릿을 **복제해 실험**할 때 참고할 `.aanb` 등만 둔다. 실행으로 생성된 프로젝트 전체는 기본적으로 **`samples/_run/`** 에 두고(`.gitignore`), 저장소에는 올리지 않는다.

| 파일 | 용도 |
|------|------|
| [`python-qt6-calchex.aanb`](python-qt6-calchex.aanb) | Python + Qt6 상용 워크플로 샘플(설계→구현·테스트·배포 준비 스크립트와 연계) |

전 주기 설계 노트북 **정본**은 `docs/agent-notebook/` 아래를 본다.

- 일반 SW: `design-principles-full-stack-workflow.aanb`
- AI·ML 포함: `ai_sw_design_full_stack_workflow.aanb`
